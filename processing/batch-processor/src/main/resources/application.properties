# Batch Processor Configuration
# Lambda Architecture - Batch Layer

# ============== PostgreSQL Configuration ==============
# Source: Read stream transfers
# Target: Overwrite with batch corrections
jdbc.url=jdbc:postgresql://localhost:15432/chainrisk
jdbc.user=chainrisk
jdbc.password=chainrisk123

# ============== Neo4j Configuration ==============
# Target: Overwrite stream graph data
neo4j.uri=bolt://localhost:17687
neo4j.user=neo4j
neo4j.password=chainrisk123

# ============== Job Configuration ==============
# Network to process
network=ethereum

# Enable/disable Neo4j sink
enable.neo4j.sink=true

# ============== Spark Configuration ==============
# These are typically set via spark-submit
# spark.master=local[*]
# spark.app.name=Lambda Batch Layer
# spark.executor.memory=2g
# spark.driver.memory=1g
