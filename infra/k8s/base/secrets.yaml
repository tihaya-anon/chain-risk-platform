apiVersion: v1
kind: Secret
metadata:
  name: chain-risk-secrets
  namespace: chain-risk
type: Opaque
stringData:
  # PostgreSQL (placeholder - use sealed-secrets or external-secrets in production)
  PG_USER: "postgres"
  PG_PASSWORD: "changeme"

  # Neo4j
  NEO4J_USER: "neo4j"
  NEO4J_PASSWORD: "changeme"

  # Redis
  REDIS_PASSWORD: ""

  # API Keys (placeholder)
  ETHERSCAN_API_KEY: ""
  BINANCE_API_KEY: ""
