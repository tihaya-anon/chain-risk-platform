apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true

  - name: PostgreSQL
    type: postgres
    uid: postgresql
    url: postgres:5432
    database: chainrisk
    user: chainrisk
    secureJsonData:
      password: chainrisk123
    jsonData:
      sslmode: disable
